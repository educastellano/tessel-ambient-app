import assign  from 'object.assign'

let initialState = {

}

let reducers = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    } 
}


export { reducers as default }
